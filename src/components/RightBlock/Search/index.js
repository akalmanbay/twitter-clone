export default function Search({ searchTweetsByText, onChangeTextInput, content }) {
    return (
        <div className='p-3 d-flex flex-column' style={{ borderBottom: '2px solid whitesmoke', height: '20px', width: '20px' }}>
            {/* <div >
                <img src={ } style={{ width: 50, height: 50, borderRadius: 50 }} />
                <input onChange={onChangeTextInput} value={content} placeholder='lalala' style={{ border: 'none', fontSize: 18, outline: 'none' }} className='mx-3' />
            </div>
            <button onClick={() => searchTweetsByText(content)} style={{ alignSelf: 'flex-end', color: 'white', backgroundColor: '#1D9BF0', borderRadius: 20, height: 40, width: 75, border: 'none' }}>Tweet</button> */}
            SEARCH</div>
    )
}